This is the first build of Start Assistant.

It has some known issues:
  1. Opening command prompt
  2. Sometimes pop-up windows get hidden

Installation setup:
Copy this folder: startassistant_build_v1/StartAssistant_v1
Unzip it
You can start it by launching the gui.exe, ignore the command line
